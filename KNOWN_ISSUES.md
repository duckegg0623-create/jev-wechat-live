# 已知问题与实测数据

> **一句话结论**：管线和工程部分是通的，**判断部分是不准的**。
> Jev 能做「答案事先已知、重复高频的分类」，做不了「读懂言外之意」。

这份文档记录我们踩到的所有坑，以及我们**为什么认为这个方向走不通**。
如果你是冲着「用 Jev 做一个懂人心的助手」来的，建议先看完第 3 节再决定要不要动手。

---

## 0. 目录

1. [Jev 是什么，官方说它擅长什么](#1-jev-是什么官方说它擅长什么)
2. [我们想让它干什么](#2-我们想让它干什么)
3. [实测：11 条真实消息的逐条对照](#3-实测11-条真实消息的逐条对照)
4. [三个系统性退化](#4-三个系统性退化)
5. [A/B 实验：上下文长度不是根因](#5-ab-实验上下文长度不是根因)
6. [我们猜的原因](#6-我们猜的原因)
7. [其他工程坑](#7-其他工程坑)
8. [我们试过但没用的](#8-我们试过但没用的)
9. [如果你要接手](#9-如果你要接手)

---

## 1. Jev 是什么，官方说它擅长什么

**Jev**（TypeSafe System One）是 OpenRouter 上一个不走 chat completions 的特殊模型：

- 端点：`POST https://openrouter.ai/api/alpha/decisions`
- 模型名：`~typesafe/jev-latest`（实际路由到 `typesafe/jev-1.13-20260917`）
- **它不生成文字**，只输出带概率的结构化判定

三种题型：

| 类型 | 输入 | 输出 |
|---|---|---|
| `noul` | 一个是/否问题 | 0~1 的概率 |
| `choice` | 一个 criteria map | 选中的 key + 全部选项的概率分布 |
| `score` | 一个有序档位列表 | 档位索引的加权均值 |

**官方给它的定位**（我们查证过，这是关键）：

> 适合 **repeated, high-volume, and the possible answers are known in advance**
> 的决策场景。

目标用例是 **routing / classification / fuzzy branching / guardrailing**。
主流用法是 inbox triage（邮件三分类）、intent routing、model routing。

它的优势是**速度（约 193×）和成本（约 444×）**、零输出 token 费用、
以及对同一个输入给出**确定性**的输出（不像 LLM 每次采样结果会漂）。
训练方法是 RLCD（Reinforcement Learning for Calibrated Decisions）——
**校准过的决策**，不是理解。

**所以它从一开始就不是为「理解」设计的。** 这一点我们在项目做到一半才真正想明白。

---

## 2. 我们想让它干什么

做一个微信实时浮层：对方发来一条消息，1 秒内判定七个维度：

| 判定 | 类型 | 选项数 |
|---|---|---|
| `literal_same` | noul | 字面 = 本意吗 |
| `emotion` | choice | 8（平静/开心/烦躁/低落/兴奋/敷衍/困惑/认真） |
| `tone` | score | 5 档（冷淡生分 ↔ 热络亲密） |
| `real_intent` | choice | 8（问信息/求助/分享告知/吐槽/开玩笑/约时间/闲聊/给反馈不满） |
| `relationship` | choice | 4（轻松/正常/拘谨/明显不对劲） |
| `need` | choice | 6（要答案/要方案/要倾听/要表态/要陪同/不需要什么） |
| `best_action` | choice | 8（直接答/追问澄清/给方案/接情绪/开玩笑/婉拒/先不接/先查证） |

**说白了就是想要「对方这句话到底什么意思」。** 这已经超出了分类，属于理解。

---

## 3. 实测：11 条真实消息的逐条对照

### 测试怎么做的

拿一段真实微信对话（对方是朋友，知道我在测这个工具，属于**口语化的熟人闲聊**），
挑出 11 条有代表性的消息，逐条丢给 Jev，把它七个维度的判定和**实际发生了什么**对照。

> **关于例句**：为确保不泄露对话内容，涉及人名、游戏名、具体事件的句子
> 都改写成等价的虚构例句（保持句长和语气类型），时间戳一律是虚构值。
>
> **Jev 的判定结果和概率保持原样。** 它的失败模式和具体句子无关 ——
> 是下面第 4 节总结的那种系统性退化。想自己复现的话，
> 跑 [`experiments/quality_test.py`](experiments/quality_test.py)。

### 结果

| # | 例句（已改写） | 类型 | Jev 情绪 | Jev 真实意图 | Jev 怎么回 | 实际 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | `🤔` | 单 emoji | — | 给反馈·不满 46% | — | 就是在想事情 | ❌ |
| 2 | 「那你到底想干嘛啊」 | 带火气的质问 | 烦躁 96% | 给反馈·不满 50% | 先追问澄清 7x% | 急了，催你把话说清楚 | 🟡 |
| 3 | 「我这不是在帮忙吗」 | 自辩、解释 | **烦躁 59%** | **给反馈·不满 68%** | 先追问澄清 | 解释、自辩，是善意 | ❌ |
| 4 | 「所以识别到了吗」 | 追问结果 | 困惑 50% | 问信息 62% | — | 追问结果 | ✅ |
| 5 | 「这ai没水平」 | 熟人调侃 | **烦躁 100%** | 给反馈·不满 72% | — | 调侃式下结论，无恶意 | ❌ |
| 6 | 「如果是真的他们早就笑死了」 | 替对方设想 | **低落 42%** | 开玩笑 46% | — | 调侃式设想 | ❌ |
| 7 | 「但是ai理解不了」 | 下判断 | 认真 41% | 给反馈·不满 54% | 先追问澄清 | 陈述判断 | 🟡 |
| 8 | 「还是没水平啊」 | 事后吐槽 | 烦躁 97% | 给反馈·不满 91% | 先追问澄清 | 调侃式吐槽 | 🟡 |
| 9 | 「看不懂」 | 短句求助 | 困惑 100% | **给反馈·不满 43%** | 先追问澄清 | 求助，要你解释 | ❌ |
| 10 | 「我喜欢你」 | 玩梗 | **认真 41%** | **分享·告知 85%** | — | 朋友间玩梗 | ❌ |
| 11 | 约 200 字的游戏安利长文 | 长段子 | 兴奋 49% | 开玩笑 65% | — | 对方在配合我测试工具 | ❌ |

**11 条里，我们认为完全判对 1 条，半对 4 条，判错 6 条。**

### 语气维度错得也很明显

| 例句 | Jev 语气 | 应该是 |
|---|---|---|
| 「这ai没水平」（熟人调侃） | **1.3 / 5 冷淡、生分** | 熟络、随意 |
| 「我喜欢你」（玩梗） | 4.9 / 5 热络、亲密 | 它把玩笑当成了表白 |

---

## 4. 三个系统性退化

上面那张表看着像是「随机出错」，但把 11 条横过来看，**错法高度一致**：

### 退化 1：「怎么回」坍缩成「先追问澄清」

`best_action` 有 8 个选项，但 11 条里 **6 条**（#2 #3 #5 #7 #8 #9）都指向
`ask_clarify`（先追问澄清），置信度 54%~76%。

这 6 条里**没有一条**真的需要追问澄清 —— 有 2 条是对方在自辩，2 条是调侃，
1 条是下判断，1 条是求助。

`ask_clarify` 是这 8 个选项里**最安全、最不需要理解**的那个：任何一句含义不明的话，
选它都不会「错得太离谱」。**它退化成了默认值。**

### 退化 2：情绪被「烦躁」污染

11 条里 **5 条**判成烦躁，其中「这ai没水平」给了 **100%**。

而这段对话的实际氛围是**熟人之间的互相打趣**，没有任何一条是真的在发火。
唯一真正带火气的那条（#2）反而是判得最准的（96%）。

**它把「口语化」「随意」「有脏字」「带评价」统统读成了攻击性。**
这在熟人闲聊里是致命误判 —— 朋友之间说话就是这个样子。

### 退化 3：短消息基本判不了

- `🤔` → 给反馈·不满 46%
- 「看不懂」→ 给反馈·不满 43%
- 「还是没水平啊」→ 给反馈·不满 91%

**没有上下文可抓的时候，它就往「给反馈·不满」这个大类里塞。**
和退化 1 是同一个症状：找一个安全的大类兜底。

---

## 5. A/B 实验：上下文长度不是根因

### 起因

长段子那条（#11）我们一开始以为判错是因为**上下文不够**：

「对方在配合我测试工具」这个关键线索，出现在目标消息**51 分钟之前**。
而我们当时的上下文窗口是 **150 字**，从目标往前数 67 字就到头了
—— 那三句线索根本在窗口外面。

**假设**：把线索喂进去，判定就会变对。

### 做法

同一条消息，跑两次，只改上下文：

- **A**：150 字窗口实际能取到的 12 条（看不到更早那三句）
- **B**：A + 更早的三句测试线索

### 结果

| 维度 | A（150 字） | B（+三句线索） | 变化 |
|---|---|---|---|
| 真实意图 | 开玩笑 **76%** | 开玩笑 **70%** | -6% |
| 怎么回 | 开个玩笑 65% | 开个玩笑 62% | -3% |
| 情绪 | 兴奋 74% | 兴奋 68% | -6% |

**假设被推翻。** 把关键线索直接喂到嘴边，Jev 的判定几乎没变，
只是概率稍微松动了一点。它还是坚持「这是开玩笑」。

### 这说明什么

Jev 是在**对当前这句话做字面分类**，不是在做多步推理
（「他为什么说这个 → 因为他在配合测试 → 所以他不是开玩笑」）。

**把线索给它，它也用不上。** 上下文能帮它消歧（代词指谁、隔了多久），
但帮不了它理解语境。

---

## 6. 我们猜的原因

**以下是我们自己的判断，没有任何官方依据，很可能是错的。欢迎反驳。**

### 主因：**选项太少，而且选项的划分方式是错的**

这是最可能的原因。

`choice` 类型的题目，**Jev 必须在 criteria map 里选一个**。
它不能回答「以上都不是」，也不能回答「这个我判不了」。

我们的 8 个 `real_intent` 选项是按**语言学意义上的意图分类**写的：
问信息、求助、分享、吐槽、开玩笑、约时间、闲聊、给反馈。

但真实对话里的一句话，往往不属于任何一个 —— 它是**两个人之间正在发生的事**：

- 「我这不是在帮忙吗」不是在「给反馈」，是在**自辩**（这个选项我们没有）
- 「这ai没水平」不是在「表达不满」，是在**跟熟人打趣**（这个选项我们也没有）
- 「我喜欢你」不是在「分享告知」，是在**玩梗**（同样没有）

**没有对应的选项时，它只能挑一个看起来最接近的。** 于是：

- 自辩 → 落进「给反馈·不满」（因为都带着一点情绪）
- 打趣 → 落进「给反馈·不满」（因为都带评价）
- 玩梗 → 落进「分享·告知」（因为字面上确实在告知一件事）

**同一个「给反馈·不满」在 11 条里被选中 6 次，就是这个原因**——
它不是判断的结果，是**兜底的结果**。

### 次因：这个任务本身就不是分类任务

退一步说，就算把选项补全，可能也解决不了。

「对方这句话到底什么意思」**没有封闭的答案集** —— 它取决于两个人的关系史、
正在进行的这件事、以及说话人此刻的状态。这不是「答案事先已知」的场景，
而 Jev 的全部优势（快、便宜、确定）恰恰建立在「答案事先已知」上。

**我们在用一个分类器做理解任务，还要它解释为什么这么分。** 从根上就拧着。

### 也许有用的方向（没试过）

1. **加一个「判断不了」的选项**，看它会不会更诚实地使用 —— 这是最便宜的实验
2. **把选项改成按「这句话在做什么」划分**，而不是按语言学的意图类别划分
3. **让它只做它擅长的那部分**：从一长段对话里挑出「需要我注意的消息」
   （二分类，答案封闭），把「怎么理解」交给 LLM
4. **换思路：让 LLM 做理解，用 Jev 做路由** —— 比如用 Jev 决定这条消息值不值得
   花 LLM 的钱去分析。这才是它官方定位的用法（model routing）

---

## 7. 其他工程坑

这些和判定质量无关，但都是真踩过的，对做同类项目的人可能有用。

### 7.1 畸形的 fallback URL 会把真实错误盖掉

配置里曾经有一个 `endpoint_fallback`，指向 `https://openrouter.ai/api/v1/api/alpha/decisions`
—— 注意 `/api` 重复了，这个 URL **永远返回 404**。

它的唯一实际作用是**把真实错误覆盖掉**：上游返回 503 时，报出来的是它的 404，
看起来像端点写错了，其实是服务在抖。我们为此排查了很久。

**修法**：删掉 fallback，并且**把每次重试的错误攒起来一起报**，
而不是只留最后一次。

### 7.2 上游 503 / 529 是常态

实测过的两种抖动：

- `503 no healthy upstream`
- `529 system_overloaded`

同一条请求隔几秒重试就能过。**必须加重试**，否则抖一下这条消息就白解读了
—— 对方不会因为你这边报错就重发一遍。

> 注意：529 并不是秒回的（我们一开始以为它是），实测要 5~7 秒才返回，
> 成功的那次花了 12.6 秒。把退避时间设得太短会一直撞在同一堵墙上。

### 7.3 微信消息库的坑（详见 `wechat-live/README.md`）

- **只盯主库 mtime 会漏掉所有新消息** —— WAL 模式下写入只改 `-wal`，
  主库要等 checkpoint 才更新。实测滞后 43 分钟。
- **用时间戳做游标会漏消息** —— 同一分钟内连发的消息时间戳完全相同，会被整批漏掉。
  改用 `local_id`（表内自增）。
- **用行号当 rowid 会认错人** —— `Name2Id` 表没有显式 id 列，必须 `SELECT rowid, user_name`。
- **同一个会话的表可以在多个 `message_*.db` 里同时存在** —— 新库建好后老表不删，
  只是冻结。按文件大小排序取第一个，会永远停在老库上，新消息一条都看不见。
- **微信新建分片库后要重新提取密钥** —— `all_keys.json` 只是某一次的快照。

### 7.4 提示词里「别用上下文」会让模型彻底无视上下文

`_ONLY_TARGET` 原本写的是「recent_context 仅作背景参考……**不要把它的问题算到
target_message 头上**」。本意是防止把上一条的旧账算到当前这条，
实际效果是**它连指代都不解析了** —— 给一句「就那个」，它不知道「那个」指什么。

**否定的措辞要小心：模型分不清「别用上文做结论」和「别用上文」。**
改成正面说明上下文该怎么用，只在最后收一句「结论只描述这一条」。

---

## 8. 我们试过但没用的

| 尝试 | 结果 |
|---|---|
| 把上下文窗口从 150 字加大 | 判定几乎不变（见第 5 节 A/B） |
| 把关键线索直接喂进去 | 同上 |
| 重写 `_ONLY_TARGET`，改成正面说明上下文用法 | 指代解析变好了，**准确率没变** |
| 修复「只看这一条把最直接的前文也排除掉」的 bug | 上下文确实进去了，**判定没变好** |
| 加重试、修 fallback、修上游抖动 | 稳定性变好了，**准确率没变** |

**工程问题一个个修完了，判定质量原封不动。** 这是让我们最终决定停在这里的原因。

---

## 9. 如果你要接手

这个项目**没有死**，只是我们走到了一条死路上。代码是完整可跑的：

- ✅ 微信 4.x 数据库解密、增量读取、WAL 处理 —— 全部正常工作
- ✅ 消息碎片合并、动态静默窗口、上下文切段 —— 正常工作
- ✅ 浮层 UI、换人搜索、暂停恢复、DPI 适配 —— 正常工作
- ✅ Jev API 客户端、重试、成本统计 —— 正常工作
- ❌ **判定准确性 —— 不达标**

**最有价值的下一步**，按我们的判断排序：

1. **加「判断不了」选项的对照实验** —— 最便宜，一次改动就能验证第 6 节的主因假设
2. **重新设计问题集的划分方式** —— 按「这句话在做什么」而不是「语言学的意图类别」
3. **换任务**：别让它理解，让它**筛选**（哪些消息值得关注），把理解交给 LLM
4. **换架构**：LLM 做理解，Jev 做路由（决定要不要花 LLM 的钱）—— 官方定位的用法

如果你做出了任何进展，或者认为我们第 6 节的判断是错的，**非常欢迎开 issue 打脸**。
